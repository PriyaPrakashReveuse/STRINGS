#include <iostream>
#include<bits/stdc++.h>
using namespace std;

int main()
{
    string s="bac.ant";
    int count=0,start=0;
    
    for(int i=s.length()-1;i>=0;i++)
    {
        while(s[i]!='.')
        {
            start=i;
            i--;
            count++;
            if(s[i]=='.')
            {
                for(int i=start;i<count;i++)
                {
                    cout<<s[i]<<endl;
                }
            }
        }
        
    }
    //cout<<s<<endl;
        
    return 0;
}
